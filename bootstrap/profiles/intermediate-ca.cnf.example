# Copy to intermediate-ca.cnf and customize for your lab. Local *.cnf files are gitignored.
# Used to create the issuing CA CSR and to apply extensions when the bootstrap root signs it.
# Policy: https://github.com/ffbarrie/my-cloud/blob/main/docs/adr/0003-pki-certificate-naming.md
# Runbook: ../software-root-ca.md

[ req ]
default_bits       = 4096
prompt             = no
default_md         = sha256
distinguished_name = req_distinguished_name
req_extensions     = v3_intermediate_ca

[ req_distinguished_name ]
CN = My Cloud Issuing CA
O  = My Cloud
OU = PKI

[ v3_intermediate_ca ]
basicConstraints     = critical, CA:true, pathlen:0
keyUsage             = critical, keyCertSign, cRLSign
subjectKeyIdentifier = hash
